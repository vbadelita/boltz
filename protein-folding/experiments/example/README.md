# Example experiment

This directory is a placeholder for experiment-specific preparation logic and source
inputs. Real experiments should write prepared YAML files plus queue job JSON files
into the shared queue.
